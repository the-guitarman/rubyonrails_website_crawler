<%= form_with(model: user_agent, local: true) do |form| %>
  <% if user_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_agent.errors.count, "error") %> prohibited this user_agent from being saved:</h2>

      <ul>
        <% user_agent.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
